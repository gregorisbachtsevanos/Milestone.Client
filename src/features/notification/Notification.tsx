import { StyledNotificationContainer } from "./Notification.styled";

const Notification = () => {
  return <StyledNotificationContainer> d</StyledNotificationContainer>;
};

export default Notification;
